# Optional local overrides (CLI compose). Portainer does not need this file —
# defaults live in docker-compose.yml environment blocks.

COMFYUI_BASE_URL=http://192.168.10.31:8188
LLAMA_BASE_URL=http://192.168.10.31:9292/v1
LLAMA_MODEL=LiquidAI/LFM2-2.6B-Exp-GGUF:Q4_K_M
LLAMA_API_KEY=not-needed
# Optional: override context budget / completion size (0 n_ctx = auto/default 8192 until set in Settings)
# LLAMA_N_CTX=16384
# LLAMA_MAX_TOKENS=2048

CHUNK_FRAMES=33
OVERLAP_FRAMES=12
DEFAULT_FPS=24
DEFAULT_WIDTH=1280
DEFAULT_HEIGHT=704
DEFAULT_STEPS=20
DEFAULT_CFG=1.0
DEFAULT_SAMPLER=euler
DEFAULT_SCHEDULER=simple

CORS_ORIGINS=*
