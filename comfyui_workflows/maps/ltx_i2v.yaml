workflow: ltx_i2v.json
description: >
  LTX image-to-video (start-only continue fallback). Node IDs empty until
  api/ltx_i2v.json is exported from ComfyUI — do not invent bindings.
model_files: []
fields: {}
expected_fields:
  - positive_prompt
  - negative_prompt
  - seed
  - steps
  - cfg
  - sampler_name
  - scheduler
  - num_frames
  - width
  - height
  - fps
  - filename_prefix
  - start_image
